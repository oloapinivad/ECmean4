# Create the ECmean4 environment:
#   conda env create -f environment.yml

name : ECmean4
channels:
  - conda-forge
dependencies:
  - python>=3.8,<3.11
  - numpy
  - netcdf4 
  - dask
  - xarray
  - xesmf
  - esmpy<=8.3.1
  - matplotlib-base
  - metpy
  - tabulate
  - numba
  - cfgrib
  - seaborn
  - pytest
