# Create the ecmean environment:
# mamba env create -f environment.yml 
# or 
# conda env create -f environment.yml

name : ecmean
channels:
  - conda-forge
dependencies:
  - python>=3.9,<3.15
  - xesmf
  - eccodes
  - pip
  - pip:
    - -e .

