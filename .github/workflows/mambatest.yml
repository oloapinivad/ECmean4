# This workflow will install Python dependencies using Mamba, 
# run tests and lint with a single version of Python

name: mamba-test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: "0 2 * * 1" #run every Monday night at 2AM UTC

permissions:
  contents: read
  checks: write
  statuses: write
  pull-requests: write

jobs:
  ruff-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.14"
    - name: Install ruff
      run: pip install ruff
    - name: Lint and format check with ruff
      run: ruff check .
    - name: Check code formatting with ruff
      run: ruff format --check .

  mamba-test:
    runs-on: ubuntu-latest
    needs: ruff-check
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]
        #python-version: ["3.14"]
    defaults:
      run:
        shell: bash -el {0}
    steps:
    - uses: actions/checkout@v6
    - name: provision-with-micromamba
      uses: mamba-org/setup-micromamba@v2
      with:
        environment-file: environment.yml
        environment-name: ecmean
        cache-downloads: true
        cache-environment: false
        post-cleanup: environment
        condarc: |
          channels:
            - conda-forge
        create-args: >-
            python=${{ matrix.python-version }}
    - name: Upgrade pip and build tools
      run: python -m pip install --upgrade pip setuptools wheel
    - name: Install package and test dependencies from pyproject.toml
      run: python -m pip install -e ".[tests]"
    - name: Run coverage
      run: |
        coverage run --source=ecmean -m pytest
        coverage combine
        coverage report
        coverage lcov
    - name: Upload coverage to Coveralls
      if: matrix.python-version == '3.14'
      uses: coverallsapp/github-action@v2
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        path-to-lcov: coverage.lcov
        debug: true
